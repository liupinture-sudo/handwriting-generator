你现在要继续维护一个单文件 HTML 项目：`index.html`。

请严格遵守以下规则：

## 一、绝对不能动的内容

1. 不要重写整个网页。
2. 不要删除或替换内嵌的 `@font-face` base64 字体。
3. 不要删除字体库预选功能。
4. 不要删除或削弱以下 V6 功能：
   - 字体库预选
   - 上传 TTF / OTF 后真实应用到 Canvas
   - `renderHandwriting()`
   - 工整度逻辑
   - 自然抖动
   - 墨迹深浅
   - 字间距
   - 中文标点避头避尾
   - PNG 导出
5. 不要引入 React、Vue、构建工具或后端。
6. 不要把单文件项目拆成多个文件，除非我明确要求。

## 二、当前项目目标

这是一个“文档转手写图片”的网页。用户输入文字或上传 TXT 后，选择字体和参数，通过 HTML5 Canvas 生成手写风格图片，并支持 PNG 导出。

## 三、已有核心逻辑

请优先查找并保护这些区域：

- `state`
- `fontMap`
- 字体库按钮或字体卡片
- `renderHandwriting(...)`
- `applyPreview()` 或类似预览渲染函数
- Canvas 绘制函数
- 下载 PNG 函数
- 滑块事件绑定区域

## 四、修改方式

当我提出需求时，你必须：

1. 先说明你准备改哪几个函数或代码区域。
2. 只改与需求直接相关的代码。
3. 保留原有功能、参数范围和视觉强度。
4. 修改后说明：
   - 改了什么
   - 没有动什么
   - 如何测试

## 五、重点风险

之前的问题是：优化卡顿时破坏了字体库、工整度和调节幅度。以后严禁用“轻量版渲染”替代正式渲染。性能优化只能做事件节流/防抖，最终仍必须调用完整的 V6 渲染逻辑。

## 六、默认测试项

每次修改后，请帮我检查：

1. 四个内置字体是否还能切换。
2. 上传 TTF/OTF 后是否还能应用到 Canvas。
3. 工整度调整是否仍有明显变化。
4. 自然抖动是否仍有明显变化。
5. 墨迹深浅是否仍有明显变化。
6. 字间距、字号、行距是否仍能正常改变。
7. 标点符号是否不会出现在行首。
8. PNG 导出是否正常。
