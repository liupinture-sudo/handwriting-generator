# 后续给 Codex 的任务模板

复制下面这段给 VS Code 右侧 Codex 聊天框：

---

请只修改 `index.html` 中与【这里写你的具体功能】相关的代码，不要重写整个网页。

必须保留：
- 内嵌字体
- 字体库预选
- 上传字体功能
- V6 的 `renderHandwriting()` 逻辑
- 工整度、自然抖动、墨迹深浅、字间距的现有效果
- 中文标点避头避尾
- PNG 导出

请先定位相关代码区域，再定点修改。不要用轻量版渲染替代正式渲染。修改完成后告诉我改了哪些函数，以及怎么测试。

我的具体需求是：

【把你的新需求写在这里】

---
